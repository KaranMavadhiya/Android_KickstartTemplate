<?xml version="1.0"?>
<globals>
    <global id="srcOut" value="${srcDir}/${slashedPackageName(packageName)}/activities" />
    <global id="srcOut" value="${srcDir}/${slashedPackageName(packageName)}/fragments" />
    <global id="srcOut" value="${srcDir}/${slashedPackageName(packageName)}/dialogs" />
    <global id="srcOut" value="${srcDir}/${slashedPackageName(packageName)}/utils" />

</globals>
